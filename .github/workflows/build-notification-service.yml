name: Build kong-demo-try_test-cor-plu-for notification-service API - Generate Kong Configs

on:
  workflow_dispatch:
  push:
    branches:
      - feat/*
      - main
    paths:
      - 'notification-service/openapi.yaml'
      - 'notification-service/kong/**'
      - 'kong/plugins/**'
      - 'kong/consumers/**'
      - '.github/workflows/build-notification-service.yaml'

jobs:
  oas-to-kong:
    name: Convert OAS to Kong configurations
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup deck
        uses: kong/setup-deck@v1
        with:
          deck-version: '1.40.3'
          wrapper: false

      - name: Generate Kong Declarative Configs from OpenAPI
        run: |
          deck file openapi2kong \
            --spec notification-service/openapi.yaml \
            --output-file notification-service/kong/.generated/kong.yaml

      - name: Merge all plugins
        run: |
          deck file merge notification-service/kong/.generated/kong.yaml \
            kong/plugins/*.yaml \
            notification-service/kong/plugins/*.yaml \
            --output-file notification-service/kong/.generated/kong.yaml

      - name: Merge consumers
        run: |
          deck file merge notification-service/kong/.generated/kong.yaml \
            kong/consumers/*.yaml \
            --output-file notification-service/kong/.generated/kong.yaml

      - name: Add tags
        run: |
          cat notification-service/kong/.generated/kong.yaml | \
          deck file add-tags notification-service \
          --selector "$.services[*]" \
          --selector "$.services[*].routes[*]"  \
          --selector "$.plugins[*]" \
          --selector "$.consumers[*]" \
          --output-file notification-service/kong/.generated/kong.yaml

      - name: Apply optional patches
        run: |
          if [ -f "notification-service/kong/patches.yaml" ]; then
            cat notification-service/kong/.generated/kong.yaml | \
            deck file patch notification-service/kong/patches.yaml \
            --output-file notification-service/kong/.generated/kong.yaml
          else
            echo "No patches.yaml found, skipping patch step"
          fi

      - name: Validate Kong config
        run: |
          deck file validate notification-service/kong/.generated/kong.yaml
          echo "Validation passed"


      - name: Copy artifact
        run: |
          mkdir -p .github/artifacts/notification-service/
          cp notification-service/kong/.generated/kong.yaml \
            .github/artifacts/notification-service/notification-service-kong.yaml
          echo "Artifact copied"

      - name: Deck diff for Konnect
        id: deck-diff
        run: |
          deck gateway diff \
            --select-tag notification-service \
            --konnect-control-plane-name ${{vars.KONNECT_INTERNAL_CP}} \
            --konnect-token ${{ secrets.KONNECT_PAT }} \
            --konnect-addr ${{vars.KONNECT_ADDR}} \
            .github/artifacts/notification-service/notification-service-kong.yaml || true
          echo "Deck diff generated"


      #an artifact file will be committed only if it the Kong config differs from the previous one.
      - name: Check status of any files to be committed
        run: git status

      - name: Create PR to stage changes for the Kong Gateway in production
        uses: peter-evans/create-pull-request@v5
        with:
          title: "❗PRD❗: Staged changes for Kong Gateway PRD deployment - demo_try_test-cor-plu-for notification-service"
          labels: "PRD,notification-service"
          body: "Staged changes for deploying demo_try_test-cor-plu-for notification-service to PRD. Review the deck diff in the workflow logs."